<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dicadut.soms.mapper.DiseaseRecordMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.dicadut.soms.domain.DiseaseRecord">
        <id column="id" property="id" />
        <result column="task_id" property="taskId" />
        <result column="component_id" property="componentId" />
        <result column="bridge_id" property="bridgeId" />
        <result column="disease_id" property="diseaseId" />
        <result column="type" property="type" />
        <result column="disease_attribute_id" property="diseaseAttributeId" />
        <result column="content" property="content" />
        <result column="version" property="version"/>
        <result column="is_deleted" property="isDeleted"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <select id="selectDiseaseRecordAppList" resultType="com.dicadut.soms.dto.DiseaseRecordAppListDTO">
        select
            dr.component_id componentId,
            c.name component,
            dr.bridge_id positionId,
            b.stake_or_truss_number position,
            dr.disease_id diseaseId,
            d.name disease
        from t_disease_record dr
        LEFT JOIN t_component c on dr.component_id = c.id
        LEFT JOIN t_component cc on cc.id = c.parent_id
        LEFT JOIN t_bridge b on dr.bridge_id = b.id
        LEFT JOIN t_disease d on dr.disease_id = d.id
        where (dr.task_id,dr.component_id,dr.bridge_id) = (#{taskId},#{componentId},#{positionId})
        GROUP BY component_id,bridge_id,disease_id
    </select>

    <select id="selectFeatureFields" resultType="com.dicadut.soms.dto.DiseaseRecordAppListDTO">
        select
            dr.disease_attribute_id diseaseAttributeId,
            da.value diseaseAttributeName,
            dr.content,
            dr.type
        from t_disease_record dr
        LEFT JOIN t_disease_attribute da on dr.disease_attribute_id = da.id
        where (dr.task_id,dr.component_id,dr.bridge_id,dr.disease_id) = (#{taskId},#{componentId},#{positionId},#{diseaseId})
          and dr.type = 1
    </select>

    <select id="selectFeaturePopups" resultType="com.dicadut.soms.dto.DiseaseRecordAppListDTO">
        select
            dr.disease_attribute_id diseaseAttributeId,
            da.value diseaseAttributeName,
            dr.content,
            dr.type
        from t_disease_record dr
        LEFT JOIN t_disease_attribute da on dr.disease_attribute_id = da.id
        where (dr.task_id,dr.component_id,dr.bridge_id,dr.disease_id) = (#{taskId},#{componentId},#{positionId},#{diseaseId})
          and dr.type = 2
    </select>

    <select id="selectFeatureRadios" resultType="com.dicadut.soms.dto.DiseaseRecordAppListDTO">
        select
            dr.disease_attribute_id diseaseAttributeId,
            da.value diseaseAttributeName,
            dr.content,
            dr.type
        from t_disease_record dr
        LEFT JOIN t_disease_attribute da on dr.disease_attribute_id = da.id
        where (dr.task_id,dr.component_id,dr.bridge_id,dr.disease_id) = (#{taskId},#{componentId},#{positionId},#{diseaseId})
          and dr.type = 3
    </select>

    <select id="selectDiseasePictures" resultType="com.dicadut.soms.dto.DiseaseRecordAppListDTO">
        select
            dr.disease_attribute_id diseaseAttributeId,
            da.value diseaseAttributeName,
            dr.content,
            dr.type
        from t_disease_record dr
        LEFT JOIN t_disease_attribute da on dr.disease_attribute_id = da.id
        where (dr.task_id,dr.component_id,dr.bridge_id,dr.disease_id) = (#{taskId},#{componentId},#{positionId},#{diseaseId})
          and dr.type = 4
    </select>

    <select id="selectDiseaseVoices" resultType="com.dicadut.soms.dto.DiseaseRecordAppListDTO">
        select
            dr.disease_attribute_id diseaseAttributeId,
            da.value diseaseAttributeName,
            dr.content,
            dr.type
        from t_disease_record dr
        LEFT JOIN t_disease_attribute da on dr.disease_attribute_id = da.id
        where (dr.task_id,dr.component_id,dr.bridge_id,dr.disease_id) = (#{taskId},#{componentId},#{positionId},#{diseaseId})
          and dr.type = 5
    </select>

    <select id="selectDiseaseTexts" resultType="com.dicadut.soms.dto.DiseaseRecordAppListDTO">
        select
            dr.disease_attribute_id diseaseAttributeId,
            da.value diseaseAttributeName,
            dr.content,
            dr.type
        from t_disease_record dr
        LEFT JOIN t_disease_attribute da on dr.disease_attribute_id = da.id
        where (dr.task_id,dr.component_id,dr.bridge_id,dr.disease_id) = (#{taskId},#{componentId},#{positionId},#{diseaseId})
          and dr.type = 6
    </select>
</mapper>
